# Semantic Calibration Failure Cases

This directory is a calibration goldmine containing screenshots, replay traces, semantic outputs, and confidence outputs of actual failed OCR/semantic resolution attempts.

## Case Log

- `fail_001`: Low-contrast tilted sachet printing `INS 50O(ii)` (letter O instead of zero). Replays as UNKNOWN/UNCERTAIN.
- `fail_002`: Curved reflective label printing `msgggg`. Replays as UNKNOWN/UNCERTAIN.
